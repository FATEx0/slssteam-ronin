Repository: https://github.com/FATEx0/slssteam-ronin
Base commit: f7bb63f157ace2e80bbc37ab1af8bf9381131a77
Working-tree stability delta:
- App-info provisioning runs in the package-contained `sls-prelaunch`
  executable before Steam starts. Running it from the rtld-audit callback
  reproduced allocator corruption; the standalone phase completed cleanly.
- PackagePatch, ManifestBind, ReconcilePin, and Parental are enabled as
  optional fail-closed adapters. On Steam build 1784778118 all patterns
  resolved, both libraries mapped, package expansion and parental rewriting
  were exercised, and a two-minute SLS-only launch soak remained stable.
- ReconcilePin redirects only EvaluateConfigChanges' direct target-vector
  builder call. The entry context vector is observation-only because it can
  alias Steam's active/installed state; rewriting it caused a metadata-only
  false success during live Linux downgrade testing.
- Managed DLC quarantine observes two optional, uniquely resolved chunk
  callbacks and filters only the target plan/package-0 injection. It must never
  be broadened to the active/public comparison path.

Packaged artifacts:

e696867b2d746bfdec9dc63f71f3727cf3e1497b4b5b9f559fd0f86431d9bb68  payload/SLSsteam.so
f2758562bddf8ff4447afe78988c54cde2791518fecf3d2a7bed290b4442e06b  payload/library-inject.so
8b0bb56399d9bc281a04d45ff25ce7c70153c9d6c3451ff6d76f2c7170d71f9b  payload/sls-prelaunch
4741937bc087f392147711128d6c215d0ed7bf669f058e82f8ffddaf856f80e4  payload/ronin-control

These hashes document the built-in source snapshot. They are not a substitute
for Ronin's deferred staged third-party installer and provenance database.

Manifest history resolution uses the optional browser helper under
`assets/steamdb-history-extension/`. The helper exports only information
rendered by SteamDB in an explicitly opened browser session: build transitions,
all rendered depot sections and their categories/ownership/descriptions/Size/DL
values, branches, DLC relationships, raw depot change events, and targeted
per-depot manifest histories for shared/redistributable payloads.
`ronin-control` treats that file as an untrusted observation: it anchors the
newest build to Steam's current local depot map, verifies every transition
while walking backwards, and installs the cache atomically only if the entire
observed chain joins. DLC and shared releases inside a base-build interval
attach to its preceding build. Shared/redistributable depots are deliberately
not required in the Lua-derived anchor: stplug-in files supply decryption keys,
not a complete Steam depot topology. A shared history must instead be declared
by the root app and cover every root-build interval. It does not claim coverage
before SteamDB's first available build or across pages that failed to load.

The hosted Game Builds page exposes only whole-app build selection. It resolves
the selected build to its complete internal depot map, persists the lock, then
uses the declared `tsuki.steam.app.validate` host import to ask Steam to apply
the change. Selecting Live removes the lock and validates back to Steam's
public build. SteamDB history JSON remains an explicit page-level import.
